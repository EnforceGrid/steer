# Build
target/

# Planning notes — internal scratchpad, never committed
.planning/

# Cargo.lock is committed — Steer ships a binary, reproducible builds require it.
# (For pure libraries you would skip this and not commit Cargo.lock.)

# Local dev overrides
.cargo/config.toml

# Runtime data
*.db
*.db-shm
*.db-wal

# Config with secrets
/steer.yaml
steer.yaml.bak
test-audit.jsonl
audit.jsonl

# macOS
.DS_Store
**/.DS_Store

# Editor / IDE
.obsidian/
**/.obsidian/
.idea/
.vscode/
*.swp
*.swo
*~
.hitl-policies/
